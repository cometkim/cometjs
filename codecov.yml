flags:
  core:
    paths:
      - packages/core
  graphql-utils:
    paths:
      - packages/graphql-utils

coverage:
  status:
    project:
      default: off

      "@cometjs/core":
        target: "100%"
        flags: core

      "@cometjs/graphql-utils":
        target: "100%"
        flags: graphql-utils

ignore:
  - .yarn
